#include <stdio.h>
/**
 * main - entry point for all the code
 * Description: low level programming
 * Return: Always 0 (success)
*/

int main(void)
{
	char letter;

	for (letter = 'a'; letter <= 'z'; letter++)
	{
	if (letter != 'q' && 'e')
	putchar(letter);
	}
	putchar('\n');

	return (0);
}
